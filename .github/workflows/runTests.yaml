## Adapted from:
##    - Roassal3 - https://github.com/ObjectProfile/Roassal3/blob/36b92db2055501c87f2ccea5573fc83fb4916884/.github/workflows/runTests.yml
##    - Willow-Playground - https://github.com/ba-st/Willow-Playground/blob/2898e1afbdd5a6011721e5a640225f81ae895de2/.github/workflows/build.yml0

name: CI

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
    #tags:
     # - "v*.*.*"

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    permissions:
      contents: write
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    env:
      PROJECT_NAME: PharoEnhancements-${{ matrix.smalltalk }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}    
    strategy:
      matrix:
        smalltalk: [ Pharo64-11, GToolkit64-release ]
    name: ${{ matrix.smalltalk }}
    
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      # Prepares SmalltalkCI
      - uses: hpi-swa/setup-smalltalkCI@v1
        with:
          smalltalk-image: ${{ matrix.smalltalk }}
      - name: Load Project and Run Tests
        run: smalltalkci -s ${{ matrix.smalltalk }}
        shell: bash
        timeout-minutes: 15
      # Here we zip the result of the build to be able to keep the artefacts
      #- name: Setup tmate session
      #  uses: mxschmitt/action-tmate@v3
      - name: Package
        run: bash ${GITHUB_WORKSPACE}/scripts/package_pharo.sh ${PROJECT_NAME} ${{ matrix.smalltalk }} ~/.smalltalkCI/_builds
        
      # Save the artefact of the build under "continuous" tag
      - name: Release
        uses: softprops/action-gh-release@v2
      #  if: startsWith(github.ref, 'refs/tags/')
        with:
          tag_name: continuous
          files: ${{ env.PROJECT_NAME }}.zip
